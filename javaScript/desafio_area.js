// area = PI * raio * raio 
const PI = 3.141592;
// let raio = 10;
const raio = 10;
// let area = area = PI * raio * raio;
let area = PI * raio * raio;  // Observe só consegue resolver uma vez

area = Math.PI * raio * raio;

console.log("O resultado da area é: " + area + " m2.");
console.log(Math.PI);

// const area = PI * raio * raio;  // Observe só consegue resolver uma vez

// area = Math.PI * raio * raio;

// console.log("O resultado da area é: " + area + " m2.");
// // console.log(Math.PI);