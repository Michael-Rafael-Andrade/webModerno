// Declarar variáveis
var a = 3;
// Atentar aos nomes que será dado a variável
// Regra geral use let para criar variáveis

let b = 4;  // let = palavra reservada

var a = 30;  // Observe que a variável (var = a foi declarada duas vezes) e NÃO DEU ERRO! mas com Let sim, não permite declarar duas vezes.
// let b = 40;
b = 40;

console.log(a, b);


const c = 5;


// c = 50;  // constante não permite alterar o seu valor.
console.log(c);


// console.log(a);